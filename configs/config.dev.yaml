app:
  name: agentrun
  env: dev
server:
  host: 0.0.0.0
  port: 9080
database:
  host: localhost
  port: 5432
  name: tidewise_ai_server
  user: agentrun
  ssl_mode: disable
  connect_timeout_seconds: 5
artifact:
  root: data
