## Workflow adoption

本 change 已在 branch 内非破坏性合并 `origin/main@4b3df5c`，并只对尚未开始的未来操作采用 R0—R3 工作流。主对话已批准 adoption checkpoint `0b21f74`；历史 checkbox、Review 结论和授权边界保持不变。详细映射见 [workflow-adoption-review.md](workflow-adoption-review.md)。

## 1. Phase A：结构基础与受控 cleanup

- [x] 1.1 记录已完成人工字段 Review：`chain_node_profiles(entity_id, definition NOT NULL, boundary_note NULL)`，`theme_profiles(entity_id, definition NOT NULL, boundary_note NOT NULL)`；名称/aliases/状态复用 `entity_nodes`，不保留 position/category/unit/level/parent/market/source/observation，不建立专属 chain_node source mapping，不使用 `research_theme`。
- [x] 1.2 记录 2026-07-13 Material Proposal Change 已覆盖“保留旧 facts、复用旧 UUID/key、legacy→target 收敛”策略；未执行任何 PostgreSQL/Neo4j Write。
- [x] 1.3 记录结构专项 Review 整改范围：当前 checkpoint 仅包含目标 schema、cleanup migration 代码、完整只读 preflight 与生产入口切换；不包含候选数据、UUID/key 方案、seed 实现或关系阶段。
- [x] 1.4 测试先行重写 `000015` 静态契约，覆盖专用 session Write 授权门禁、精确目标集合、event links/entity edges、convergence/audit、physical constraint/topology/membership/source mapping/profile 的叶到根清理、未知 FK 阻断、最小 profile schema、禁止 TRUNCATE/CASCADE 与不可逆 down 边界。
- [x] 1.5 重写版本化 `000015_refactor_industry_chain_node_phase_a.sql`：编码受控 cleanup 与最小 chain_node/theme schema，只提交代码，不 apply migration、不写 PostgreSQL。
- [x] 1.6 测试先行实现 repeatable-read/read-only preflight：覆盖旧表与引用 counts、`event_entity_links`、全部 convergence/audit 子表、FK/trigger/function/view/rule catalog 引用、orphans、`entity_key` 条件门禁及非目标实体 counts/校验和。当前 checkpoint 不运行 cleanup Write。
- [x] 1.7 切换默认生产入口：普通 seed 不加载旧 sector/industry_chain/chain_node 数据文件或旧产业关系，service 拒绝旧实体、source mapping、membership/topology/constraint 与旧关系端点/类型，CLI 禁用旧 apply scopes/convergence flags；历史数据 fixture 仅存在于测试代码。
- [x] 1.8 运行 targeted tests、`go test ./...`、OpenSpec strict validation、diff/scope/secret 检查，创建并 push scoped structure implementation review checkpoint；随后停止等待主对话复验。
- [x] 1.9 记录主对话已复验 structure implementation checkpoint `0f20171`：targeted tests、`go test ./...`、OpenSpec strict validation 与 diff check 均通过；该验收只允许继续 first-batch data contract Review，不授权 migration、cleanup、seed 或任何 PostgreSQL/Neo4j Write。
- [x] 1.10 只读核验已批准工作簿 Sheet「标准化保留」并提交数据契约审阅材料：842 个互异 canonical、950 个互异原始名称、108 个同义合并、79 个宽边界审阅节点；1,156 条外部代码为 eastmoney 811、ths 345，241 个节点两侧均有代码且无跨节点代码冲突。当前不生成可执行 seed。
- [x] 1.11 **First-batch data contract Review 门禁**：主对话审阅 842 节点范围、aliases 规则、definition/boundary 生成与逐项 Review 策略、全新 UUID/entity_key、去重/幂等、dry-run/report，以及通用 `entity_external_identifiers` schema。原先对 6 条组合来源分类、13 个 code 的“逐代码 taxonomy 消歧”门禁已被用户核验事实取代：每个 code 各展开 industry/concept 两条三元 mapping；不得确定具体 theme 实例。
- [x] 1.12 **Schema/TDD implementation Review 门禁**：主对话已批准 checkpoint `a0547e6`；该批准只允许进入 1.13 Cleanup Review，不授权 migration、cleanup、seed 或任何 PostgreSQL/Neo4j Write。
- [x] 1.13 **R0 Cleanup Readiness Review package**：主对话已批准 checkpoint `8a15447` 中当前 local 开发库的标准 read-only preflight、custom-format backup、168 行冻结目标集合、逐表 counts、catalog/逻辑引用、锁/事务影响、非目标保护与 forward-fix 证据。随后命名 R2 restore rehearsal 被撤销且未执行，`backup_verified=false`；主对话已对本次 local cleanup 明确接受“稳定 backup hash/size + archive integrity/full decode + 冻结目标/非目标基线 + forward-fix”作为 recovery evidence。该 local-only 风险接受不授权 cleanup Write，也不适用于 UAT/prod 或不可替代数据。
- [x] 1.14 **R3 独立 Cleanup Authorization package -> Write -> Query/assert**：主对话已明确授权并验收命名操作 `phase-a-legacy-industry-cleanup`，checkpoint `f2bc90a` 通过。其 local migration 15 Write、写后 Query/assert、466 非目标保护与 `backup_verified=false` 历史边界均已记录；不延伸至 migration 16、seed、mapping、relation 或 Neo4j。
- [x] 1.15a **R2 条件式执行包准备：external identifier schema**：cleanup Query 已经 checkpoint `f2bc90a` 验收后，提交独立 [R2 authorization package](phase-a-external-identifier-schema-authorization.md)，包含当前 local 身份/Goose=15/16 pending、migration 16 精确 schema diff、fresh post-cleanup backup/archive evidence、零 data rows 范围、before/after assertions、target-version=16 唯一路径与 fail-closed 停止条件。package 已准备但尚未授权或执行；不授权 node/profile、mapping、relations、migration 17、Neo4j 或 UAT/prod。
- [x] 1.15b **R2 条件式执行：external identifier schema Write -> Query/assert**：主对话已批准写后验收 checkpoint `ce2136d`。实际 applied 仅 `000016`；Goose=16、`entity_external_identifiers` 为零行并具有 1 PK、1 FK、1 三列 unique、5 个 CHECK 与 1 个 entity-side 普通索引，466 个实体、12 类 checksum、`entity_edges=331`、profiles 与完整性断言不变。该验收只允许进入 1.16 R0 package，不构成 seed、mapping、relation、migration 17 或 Neo4j 授权。
- [x] 1.16 **R0 Final seed candidate Review package**：主对话已批准 commodity→产业的 16 项精确重命名与 definition/boundary 强化；重生成 manifest 后 dry-run 精确为 842 created / 0 updated / 0 unchanged / 0 conflict。13 个未决 taxonomy 仍只阻断 mapping 层。
- [x] 1.17 **R2 条件式执行：node/profile seed -> Write -> Query/assert**：首次命名层 `phase-a-chain-node-seed` 曾被已删除 `entity_convergence_alias_moves` 引用 fail-closed 阻断，未发生部分写入；经主对话批准的 R1 最小修复将 post-cleanup preflight 与通用 upsert 对齐为仅使用审核 manifest 的稳定 aliases。刷新 preflight 精确匹配 Goose=16、466 个受保护实体、331 条 `entity_edges`、零 chain_node/profile/external identifier、批准 manifest SHA-256 与稳定 backup evidence 后，唯一标准 Write 成功创建 842 个 `chain_node` 与 842 个 `chain_node_profiles`，0 updated/unchanged/failed。写后 Query/assert 确认 entity 总数=1,308、842 节点/profile、842 个 manifest identity 精确匹配、16 个 commodity→产业命名生效、12 类非目标实体/checksum 与 `entity_edges=331` 未变、所有完整性断言通过。主对话 fresh readback 进一步确认 842 definitions 非空、79 wide-boundary `boundary_note` 非空、其余 763 个按可空契约为 NULL/空；执行证据已更正并获技术验收，见 [phase-a-chain-node-seed-execution-evidence.md](phase-a-chain-node-seed-execution-evidence.md)。该验收只允许进入 1.18 R0 candidate Review，不构成 mapping Write 授权。
- [x] 1.18a **R0 Mapping candidate Review**：原始工作簿候选包冻结了 1,156 个来源代码、811/345/241 来源 counts、842 目标节点、79 宽边界与 commodity→产业绑定；它是用户核验事实的输入清单，不是最终 mapping row 计数。
- [x] 1.18a.1 **已废止的网页 taxonomy 研究记录**：checkpoint `1c63479` 的网页核查、单一 taxonomy 推荐、排除建议与 1,153 行重算均只保留为历史记录，不得覆盖工作簿事实或作为后续候选输入。
- [x] 1.18a.2 **R0/R1 final mapping candidate remediation**：在不改变 migration 16、字段、domain 或 repository 的前提下，TDD 修正生成器；13 个用户核验的组合分类 code 各展开 `industry_sector` 与 `concept_sector` 两条三元 identity，重生 [最终 mapping candidate manifest](phase-a-external-identifier-mapping-candidate-review.md) 为 1,169 rows（eastmoney=818、ths=351、双来源=241），三元/确定性 ID 唯一、节点绑定完整、orphan=0、重复生成稳定。`ready_for_write=true` 仅表示候选语义完备，不构成 R2 Write 授权。
- [x] 1.18b.1 **R0 R2 authorization Review package**：主对话已验收 1.18a.2 后，提交 [phase-a-external-identifier-mapping package](phase-a-external-identifier-mapping-authorization.md)，冻结 1,169/818/351/241/13 manifest 与 recovery/preflight/Query 断言。当前发现 mapping-only runner 尚未实现，package 明确为不可执行 Review 对象，不授权 Write。
- [x] 1.18b.2 **R1 mapping-only runner implementation Review**：主对话复验 checkpoint `fc0ac48` 后，mapping flags 集中 fail-closed、单事务全量 plan/写入/提交前断言、DB snapshot dry-run 与冻结 first-batch 合同均已通过；仅允许进入已命名 mapping R2。
- [x] 1.18b.3 **R2 mapping data Write -> Query/assert**：主对话以安全本地凭据注入执行并独立验收唯一 `phase-a-external-identifier-mapping` Write。冻结 SHA-256、1,169/818/351/241/13、Goose=16、842 node/profile、331 edge、三元/ID/target/orphan 与写后 dry-run 均通过；见 [execution evidence](phase-a-external-identifier-mapping-execution-evidence.md)。不构成 1.19、Phase B 或 Neo4j 授权。
- [x] 1.19 **R0 Phase A Acceptance Review package**：主对话已批准 checkpoint `7b90c30`；已聚合 R3 cleanup 与三个 R2 命名层的实际 pre/post evidence、未验证项和 scoped diff，见 [Phase A Acceptance Review](phase-a-acceptance-review.md)。PG cleanup 后 Neo4j 将暂时陈旧，本 change 不清理、不写入、不 rebuild；任何 Neo4j rebuild 都是本 change 外的独立 R3 授权对象。

## 2. Phase B：基于全新节点建立关系

- [x] 2.1 记录已完成人工关系语义 Review：唯一表 `chain_node_relations`，不复用 `entity_edges`；MVP 仅含 `is_subcategory_of`、`is_component_of`、`input_to`、`depends_on`，删除 `contains`、`supplies_to`、`substitutes_for`、`transmits_to`，同一机制不得同时记为 `input_to` 与 `depends_on`，事件传导动态推导。
- [x] 2.2 **R1 Phase B implementation Review package（开始）**：Phase A Acceptance 已通过；测试先行新增 relation migration/领域 table-driven tests，覆盖字段、四类枚举、方向、新 chain_node 端点 FK、自环、tuple 唯一、mechanism/condition/evidence/provenance、状态、时间与 input/depends 机制互斥。
- [x] 2.3 测试先行新增 repository/seed fixture 与 DB snapshot dry-run，覆盖 created/updated/unchanged、ID/tuple 冲突、active chain_node 端点、snake_case manifest、旧 topology/constraint 字段及旧关系类型拒绝；relation-only CLI 仅开放只读 dry-run，不提供 R2 Write 入口。
- [x] 2.4 实现 `ChainNodeRelation` 强类型契约、`chain_node_relations` / 新 physical constraint 版本化 migration、repository/validator/manifest loader/dry-run/report；新 constraint 只接受全新 node/relation subject。当前只提交代码与测试，未 apply migration、未写 PostgreSQL。
- [x] 2.5 完成 R1 implementation + R0 candidate Review package及第二遍 remediation：96 条静态语义候选由双遍 AI Review 批准为 candidate draft（95 条 `is_subcategory_of`、1 条 `is_component_of`），只表达集合从属/组成；51 条保持 blocked（47 条 taxonomy、3 条 `input_to`、1 条 `depends_on`），7 条 rejected（原 5 条 + 2 条产业 endpoint mismatch），4 条 physical constraint 保持 blocked。CLI 对非空 physical constraints fail-closed，loader 拒绝尾随 JSON，migration 017 补 subject 查询索引。主对话已确认 checkpoint `f8b3406` 的[分层 evidence contract](phase-b-evidence-contract-amendment-review.md)；候选仍不是可执行 seed。
- [x] 2.6a **R0 relation schema R2 Review package prepared**：提交命名层 [`phase-b-relation-schema`](phase-b-relation-schema-authorization.md) 的 migration 17 精确 schema diff、local 环境、范围/排除范围、fresh preflight、fresh backup/full-decode recovery evidence、唯一 target-version=17 路径、before/after assertions、幂等与 fail-closed 停止条件。package preparation 不授权或执行 Write。
- [ ] 2.6b **R2 relation schema Write -> Query/assert**：只有主对话独立验收 2.6a 并明确授权命名操作后，才允许执行 migration 17；实际 applied 只能是 `000017`，且必须立即 Query/assert 两张空表、约束、索引、FK、Goose 版本、Phase A 数据保护与幂等。该层不授权 relation/constraint data，也不得自动进入 2.7。
- [ ] 2.7 **R1 runner 技术验收 -> R2 relation data Write -> Query/assert**：仅在 schema Query 验收后，先在本 task 内自动完成 relation-only atomic batch runner、manifest/snapshot validator、单事务 rollback、提交前 assertions 与 dry-run/report 的 R1 实现和技术验收；只有可写 manifest 精确冻结后，才提交命名层 `phase-b-relation-data` 的 local 环境、范围/排除范围、recovery evidence、预计 counts、before/after assertions 与停止条件并请求人工 R2 授权。获批 Write 后立即 Query/assert 方向、端点、tuple、自环、机制冲突、evidence、孤儿、constraint subject 与幂等。
- [ ] 2.8 运行 R1/Apply-final 验证：受影响 entity foundation、migration 与 relation packages 的完整 suite 及共享 architecture/contract tests、OpenSpec strict validation、scoped diff 和 secret 检查；仅当届时触发共享规则、跨模块契约、公共基础设施或边界不明条件才运行 repo-wide `go test ./...`，并记录选择理由。明确证明未运行 Neo4j rebuild、未写 Neo4j、未加入观测/事件推理/股票推荐或联盟/国家/市场/benchmark/index 调整。
- [ ] 2.9 **Apply-final Review package**：提交 scope、non-goals、风险等级、scoped diff、完整测试与 R3/R2 各命名层的实际 pre/post evidence、未验证项、阻断项和下一步边界，停止等待主对话人工 Review；批准前不得 Sync、Archive 或 Deliver。
